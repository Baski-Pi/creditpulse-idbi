# Dashboard runtime — pinned to the tested/deployed combination.
# Full training environment lives in requirements-train.txt
streamlit==1.59.0
pandas==2.3.3
numpy==2.4.6
pyarrow==24.0.0
altair==6.2.2
